import time
import serial
 
ser = serial.Serial('/dev/ttyACM0', 9600)

arquivo = open('/root/dados.txt', 'w')
contador = 0

while (contador < 300):
     value = ser.read()	
     arquivo.write(value)
	print(value)
	print(contador)
     contador = contador + 1
arquivo.close()
